# Blog and Resource Intake Template

Use this template when sending a batch (recommended: 2-3 items).

## Batch Metadata
1. Batch name:
2. Target publish window:
3. Primary business goal:
4. Notes on priority order:

## Item Template
Copy this section for each item.

### Item
1. Working title:
2. Source content (paste full text or attach):
3. Intended audience:
4. Desired category (if known):
   - guides
   - case-notes
   - templates
   - blog-posts
   - or propose new
5. Suggested tags (optional):
6. Primary CTA destination:
   - /contact?intent=demo
   - /contact?interest=NyLi%20Assets
   - /contact?interest=NyLi%20Insights
   - /contact?interest=NyLi%20Agent
   - /support
   - other
7. Publish date (YYYY-MM-DD):
8. Reading time estimate (optional):
9. Internal links to include (optional):
10. External source links to cite (optional):
11. Tone preference (optional):
12. Any claims requiring exact wording:

## Optional Asset Direction
1. Cover image concept:
2. Preferred visual elements:
3. Avoid these visual motifs:

## Output You Will Receive
For each item, the implementation will include:
1. New blog page in `blog/insight-{slug}.html`
2. New SVG cover in `assets/insight-post-{N}.svg`
3. Resource card entry in `assets/data/resources-data.js`
4. Discovery sync updates in `insights.html` (schema and/or noscript when needed)
5. URL entry in `sitemap.xml`
6. Validation report from `node scripts/validate-static-site.mjs`
